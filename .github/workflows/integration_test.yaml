name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: yanksyoon/operator-workflows/.github/workflows/integration_test.yaml@main
    secrets: inherit
    with:
      tmate-debugging: true
